import 'package:flutter/material.dart';

// Import the firebase_core and cloud_firestore plugin

class NguoiDung {
  final String addr;
  final String avt;
  final String email;
  final String fullName;
  final String id;

  NguoiDung(this.addr, this.avt, this.email, this.fullName, this.id);
}

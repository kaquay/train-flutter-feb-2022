class Room {
  String idRoom;
  String name;
  // ignore_for_file: prefer_typing_uninitialized_variables
  var color;
  String avt;
  String device;
  var humidity;
  var temperature;
  var textColor;
  Room(this.idRoom, this.name, this.color, this.avt, this.device,
      this.textColor, this.humidity, this.temperature);
}
